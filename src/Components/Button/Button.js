import React from "react";
import "./Button.scss";

const Button = () => {
  return (
    <div className="button animated delay-1s fadeInLeft">
      <button type="button">START</button>
    </div>
  );
};

export default Button;
